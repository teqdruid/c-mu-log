/* test to find the no. solutions one gets under given constraints*/

wall(3,4);
wall(4,8);
wall(6,8);
wall($X,$Y) {AND:
    $X>=10;
    $X<=15;	
    $Y<=8;
    $Y>=2;
}
wall(){}

main ()
{
    @print("Wall: ", $x, ",", $y);
    wall($x, $y);
}

		
