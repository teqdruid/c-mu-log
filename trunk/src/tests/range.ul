/* test to find how the range cases work*/

foo($x) {
    10 > $x;
    1 < $x;
}


	
bar($y) {
    $y>9;
    19 >$y;    
} 

bar($x) {
    $x < 50;
    10 < $x;
}

main() {
    {OR:
	//foo(0);
	//foo(5);	
	//foo(6);
	bar(15);
	//foo(1);
	//bar(25);
	//bar(27);
    }
    bar(49);
		
}
/* conclusions: If an infinte range is given the interpretor gives no solution
		The corner case '=' doesnt work in >=
		the bar(15) gives 2 solutions as it comes within the overlapping range(10-18)*/

